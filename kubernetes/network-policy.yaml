apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sandbox-default-deny-egress
  namespace: academy-sandboxes
spec:
  podSelector:
    matchLabels:
      app: student-sandbox
  policyTypes:
  - Egress
  egress:
  # Dev/testing mode: Allow all outbound egress traffic
  - {}
